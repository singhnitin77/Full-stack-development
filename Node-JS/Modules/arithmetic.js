const add = function (num1,num2) {
    return num1 + num2
}

const sub = function (num1,num2) {
    return num1 - num2
}

const mul = function (num1,num2) {
    return num1 * num2
}

module.exports = {addtion: add, subtraction: sub, multiplication: mul};
